# Data and risks

- Main sample: `data/store_demand_sample.csv`
- The `promo_flag` column is safe only when it is known at `week_start`.
- Keep the benchmark narrow: a naive repeat-last-week baseline is enough for bootstrap smoke.
- If the notes remain too rough for a stronger novelty claim, keep the mission in baseline-improvement mode.
